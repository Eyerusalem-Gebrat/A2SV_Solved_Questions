from collections import defaultdict
class Solution:
    def findDuplicate(self, paths: List[str]) -> List[List[str]]:
        res = []
        contents = defaultdict(list)
        for s in paths:
            path = s.split()
            for i in range(1, len(path)):
                file_name = path[i].split(".")
                content = file_name[1]
                contents[content].append(path[0] + "/" + file_name[0] + ".txt")

        for val in contents.values():
            if len(val) > 1:
                res.append(val)
                
        return res
